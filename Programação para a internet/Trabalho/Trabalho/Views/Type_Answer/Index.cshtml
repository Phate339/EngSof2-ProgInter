@model IEnumerable<Trabalho.Models.Type_Answer>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Adicionar</a>
</p>

<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.PossibleAnswer)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PossibleAnswer)
            </td>
        </tr>
}

    </tbody>

</table>


<p>
    <a asp-controller="Surveys" asp-action="ViewResults">Concluir</a>
</p>