@model IEnumerable<Trabalho.Models.Ans_Que>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ResPer)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Question)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ResPer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Question.QuestionID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Ans_QueID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Ans_QueID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Ans_QueID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
